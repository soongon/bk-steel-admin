import { PageStub } from "@/components/admin/page-stub";

export default async function PartnersPage() {
  return (
    <PageStub
      title="거래처"
      description="공유 마스터 + 책별 신용한도(여신) + 별칭 표기 흡수"
      isShared
    />
  );
}
