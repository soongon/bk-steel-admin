# 시스템 도메인 룰 v1

> 6월 Next.js + Supabase 시스템(`bk-steel-admin`)의 도메인 룰. 5월 워크북 시점의 `docs/도메인_룰.md`를 잇는 후속 문서.
> 본 문서는 **확정된 결정**과 **미결정 항목**을 함께 기록한다. 미결정 항목은 §10 참조.

---

## 1. 3축 분리 — 책(book)

시스템 최상위 차원은 **책(book)**. 모든 거래성 row는 정확히 한 책에 속한다.

| 내부 코드 | 표시명 | 실체 | 자료성 |
|---|---|---|---|
| `bk` | **법인** | 새로 설립할 SH철강(가칭) 법인 (50:50 동업) | 100% 자료거래(원칙) |
| `sl` | **사업자** | 친구 개인사업자 SL철강 | 자료거래 원칙, 일부 무자료 가능 |
| `b` | **B계좌** | 친구 사업자의 무자료 거래 흐름 (히든 통장 기반) | 무자료 |

### 1.1 코드 vs 표시 분리

- **DB·API·URL** : `bk` / `sl` / `b` 만 사용
- **UI 출력** : 라벨 컴포넌트 하나로 통일 (`<BookBadge book="bk" />` 등)
- 라벨/색상은 `lib/book.ts` 한 파일에서 중앙 관리 → 변경 시 한 군데만

```ts
export const BOOK_LABEL = { bk: '법인', sl: '사업자', b: 'B계좌' } as const
export const BOOK_COLOR = { bk: 'blue', sl: 'amber', b: 'red' } as const
```

### 1.2 책 컨텍스트는 라우트에 박는다

```
/bk/sales         /bk/purchases     /bk/inventory   ...
/sl/sales         /sl/purchases     /sl/inventory   ...
/b/cash           /b/inventory      ...
```

새로고침·딥링크·실수 방지 + RLS 강제에 유리. 책 selector(상단)는 prefix만 교체.

### 1.3 마스터 데이터 책임 정리

| 마스터 | 결정 |
|---|---|
| 거래처 | **공유** (book별 신용한도/메모는 분리 가능) |
| 품목/규격 | **공유** |
| 통장 | **책 종속** (법인 통장 ≠ 사업자 통장 ≠ B통장) |
| 창고 | 미결정 (§10) — 공유(zone으로 책 구분) vs 책 종속 |

---

## 2. 책 간 이관 매트릭스

| From → To | **BK** | **SL** | **B** |
|---|:-:|:-:|:-:|
| **BK →** | — | ✅ 정상거래(`inter_book_transfer`) | ❌ 금지 |
| **SL →** | ✅ 정상거래(`inter_book_transfer`) | — | ✅ 비공식이관(`internal_reclass`) |
| **B →**  | ❌ 금지 | ✅ 비공식이관(`internal_reclass`) | — |

### 2.1 거래 타입

| 타입 | 사용 사례 | 자료 발행 | 부가세 영향 |
|---|---|:-:|:-:|
| `sale` / `purchase` | 외부 거래 | book별 정책 (§4) | book별 (B만 제외) |
| `inter_book_transfer` | BK ↔ SL | ✅ 필수 (특수관계자 거래) | ✅ |
| `internal_reclass` | SL ↔ B | ❌ | ❌ |
| `inventory_adjustment` | 절단·가공·재고실사 차이 | — | — |

### 2.2 핵심 룰

- **BK ↔ B 차단**: 법인이 무자료 거래에 노출되면 안 됨. DB constraint로 강제.
- **BK ↔ SL `inter_book_transfer`** : 특수관계자 거래라 **시가 산정 근거**(시세 입력값, 외부 견적서 등)를 시스템에 함께 기록.
- **SL ↔ B `internal_reclass`** : 같은 사업주(친구)의 자산 재분류라서 매출/매입 통계에 잡히지 않음. 단 audit_log + transfer_pair_id 로 이력 보존.

---

## 3. 책별 거래 정책 (자료성)

거래성 row 공통 컬럼 `is_documented bool` + `tax_doc_type` (§4) 로 강도 구분.

| 책 | 자료 비율 원칙 | 무자료 row 허용? | 부가세 신고 |
|---|---|:-:|:-:|
| **BK** | 100% | ❌ (DB constraint로 차단) | 포함 |
| **SL** | 원칙 자료, 일부 무자료 가능 | ✅ (row 단위 플래그) | `is_documented=true` row만 포함 |
| **B** | 무자료만 | 모든 row 무자료 | 제외 |

---

## 4. 세금계산서 분류

```sql
tax_doc_type enum:
  'tax_invoice_electronic'   -- 전자세금계산서 (기본값)
  'tax_invoice_paper'        -- 종이세금계산서
  'invoice'                  -- 계산서 (면세)
  'cash_receipt'             -- 현금영수증
  'simple_receipt'           -- 간이영수증
  'none'                     -- 무자료
```

### 4.1 book별 허용 범위 (DB constraint)

| 책 | 허용 |
|---|---|
| BK | `tax_invoice_electronic`, `tax_invoice_paper`, `invoice`, `cash_receipt` |
| SL | 위 전체 + `simple_receipt`, `none`(예외적) |
| B | `none` 만 |

기본값: `tax_invoice_electronic`.

---

## 5. 재고 도메인

### 5.1 모델 — 단일 ledger

별도 piece 테이블은 두지 **않는다**. 매입라인이 곧 가닥/lot 역할, 매출라인 ↔ 매입라인 allocation으로 추적.

```
purchase                                  -- 매입 헤더
purchase_line                             -- 매입 라인 = 입고 단위 (piece 역할)
  id, purchase_id, book, warehouse_id, zone, item_id
  acquired_unit  enum('ton','kg','ea','piece')
  acquired_qty   numeric
  unit_price     numeric
  length_mm, grade, ...                   -- 가닥 단위면 채움
  theoretical_weight_kg                   -- 이론중량 (가닥수×규격×길이 등)
  actual_weight_kg                        -- 실중량 (저울 계량, nullable)
  price_basis    enum('theoretical','actual')
  invoiced_weight_kg                      -- 세금계산서·거래명세표 발행 기준
  status         enum('in_stock','partial_out','depleted','transferred_out','scrapped')

sale_line
  id, sale_id, book, item_id, qty, unit, weight_kg, ...
  theoretical_weight_kg, actual_weight_kg, price_basis, invoiced_weight_kg

sale_line_allocation                      -- 어느 매입라인에서 얼마나 차감했는지
  id, sale_line_id, purchase_line_id, allocated_qty, allocated_weight_kg

inventory_adjustment                      -- 절단·가공·실사 차이 등 비거래 이벤트
  id, book, type ('cut','split','stocktake','loss','scrap'), ...
```

### 5.2 재고 = `purchase_line` − `sale_line_allocation`

뷰/MV로 노출. 책별·창고별·품목별 집계.

### 5.3 절단/가공

`inventory_adjustment(type='cut', parent_purchase_line_id=..., children=[{length, weight}, ...])`. 원본 보존, 자식 라인이 새 재고가 됨.

### 5.4 평가법

**개별법(specific identification)**. `purchase_line` ↔ `sale_line_allocation` 으로 정확히 매칭되므로 자연스럽게 개별법이 성립한다. 결산 시 원가 산정·세무조사 시 추적성 모두 최강.

---

## 6. 중량 기준 (이론중량 vs 실중량)

### 6.1 한국 철강 유통 현실

- **이론중량(theoretical)** : 규격 × 길이 × 표준비중 계산값
- **실중량(actual)** : 야적장 저울 실측값
- 압연공차(±3~5%), 길이공차, 표면 산화·페인트로 둘은 항상 다름

### 6.2 거래 방향·단위별 중량 기준 (자동 분기)

| 거래 방향 | 단위 | 시나리오 | 기본 `price_basis` | `actual_weight_kg` |
|---|---|---|:-:|:-:|
| **매입** | 톤 | 제강사 정품, 묶음 | `theoretical` | 선택 |
| **매입** | kg | 중고철근 등 | `actual` | **필수** |
| **매입** | 가닥/EA | H빔·각파이프 등 | `theoretical` | 선택 |
| **매출** | 톤/EA/가닥 | 거의 모든 매출 | `theoretical` | 선택 |

사용자가 거래마다 override 가능. 차이(`weight_diff = actual − theoretical`)는 자동 계산되어 거래처별 평균 차이를 메트릭으로 노출.

### 6.3 재고 자산가치 평가

- 실중량(`actual_weight_kg`)이 있으면 그 값 기준
- 없으면 이론중량 기준
- 평가액 = 잔량 × 그날의 시세 (§7)

---

## 7. 시세(price_history) 정책

> 외부 자동수집은 신뢰·법적 이슈가 있고, 매입가만 쓰면 데이터 공백, 100% 수동은 부담. → **혼합 모델**.

### 7.1 데이터 모델

```sql
market_item                               -- 시장 표준 분류 (시세 단위)
  id, code, label_ko, category, default_unit

item.market_item_id                       -- 우리 품목이 어느 market_item에 속하는지

price_history
  id, market_item_id, recorded_on (date)
  price_per_unit, unit ('kg'|'ton')
  price_type enum('wholesale','retail','scrap','spot')
  source enum('manual','external','purchase_derived')
  source_label, source_url
  recorded_by, notes
  unique (market_item_id, recorded_on, source, price_type)
```

### 7.2 데이터가 채워지는 3가지 경로

| 경로 | 방식 | source |
|---|---|---|
| A. 수동 입력 | "오늘의 시세" 페이지에서 사용자가 직접 | `manual` |
| B. 매입가 자동 누적 | 매입 등록 시 그 단가가 동일 날짜로 자동 row 추가 | `purchase_derived` |
| C. 외부 시세 import (v2 예정) | CSV/paste 업로드 (스틸데일리 등) | `external` |

### 7.3 "오늘의 시가" 선정 우선순위

`manual` > `external` > `purchase_derived`. 같은 날 여러 source 공존 가능 (unique 키에 source 포함).

### 7.4 입력 UX — "오늘의 시세" 페이지

- `/admin/prices/today` (책 무관, 공유 페이지)
- 즐겨찾는 8~12개 품목군 한 화면 노출
- "어제와 동일", "일괄 +%" 버튼 등 빠른 입력
- 미입력 날은 가장 최근값을 carry-over하여 대시보드 표시

---

## 8. 대시보드 KPI 후보

### 8.1 시세 패널
- 주요 품목 시가 카드 (현재가, 7일/30일 변동률)
- 시세 시계열 (시가/매입평균/매출평균 3선)
- 보유 재고 시가 평가 (책별, BOM 가치 대비 변동)
- 마진 압박 지표 (매입 평균가 vs 현재 시가)

### 8.2 운영 패널
- 책별 매출/매입 추이 (월별)
- 미수 등급별 합계 (정상/단기/중기/장기)
- 만기 7일 이내 어음·외상 알림
- B계좌 입출금 일자별 (별도 패널, 시각 격리)
- 책 간 이관 잔액 (BK↔SL 이관 누적, 정산 대조)
- 재고 회전율

---

## 9. 부대원가 / 결제 / 거래처 신용 — 정책

| 항목 | 결정 |
|---|---|
| **부대원가**(운반비·하역비 등) | v1에서 매입원가에 분배하지 않음. 별도 비용으로만 기록 (혹은 추후 v2 검토). |
| **재고 평가법** | 개별법(specific identification) — §5.4 |
| **분개 자동 생성** | 미결정 (§10) — v1에서는 외부 회계 프로그램으로 export 형태로 가능성 검토 |

---

## 10. 미결정 (Open Questions)

다음 결정 후 `docs/시스템_DB_스키마_v1.md` 초안으로 진행.

| # | 영역 | 옵션 |
|---|---|---|
| 1 | **창고 마스터** | (a) 공유 + zone으로 책 구분 / (b) 책 종속 |
| 2 | **SL의 무자료 거래** | (a) SL 책 내 `is_documented=false` 플래그로 둠 / (b) 실질적으로 B 책으로 분류 |
| 3 | **`internal_reclass`(SL↔B) 가격 산정** | 시가 / 매입원가 / 무가(0원) 중 어느 것? |
| 4 | **이관 시 시가 근거 첨부** | 필수로 강제 / 권고만 |
| 5 | **시세 입력 UX** | "오늘의 시세" 페이지 (§7.4) 채택 여부 + 큐레이션할 품목군 8~12개 선정 |
| 6 | **`market_item` 분류 체계** | (a) 본인 정의 / (b) 한국철강협회·스틸데일리 분류 따르기 |
| 7 | **매입가 자동 누적(`purchase_derived`)** | ON / OFF — 추천 ON |
| 8 | **외부 시세 import** | v1에 포함 / v2로 보류 — 추천 v2 |
| 9 | **재고 시가 평가** | 보유 piece 무게 × 그날 시가, 책별 노출 — 채택 여부 |
| 10 | **분개 자동 생성** | 시스템 내부에서 더블엔트리 생성 / 외부 회계 프로그램으로 export만 |
| 11 | **선급/예약 상태** | 발주중·예약중 재고를 별도 추적할지 |
| 12 | **반품·클레임 처리** | v1 포함 / v2 보류 |
| 13 | **할인/리베이트** | 사후 단가 조정 패턴 — v1 포함 여부 |
| 14 | **품질 등급 재분류** | v1 포함 / v2 보류 |
| 15 | **위탁보관/타사 야적** | v1 포함 / v2 보류 |
| 16 | **거래처 신용한도(여신)** | 매출 시 미수 누적이 한도 초과면 경고 — v1 포함 여부 |

---

## 11. 마이그레이션 경로 (5월 → 6월)

```
docs/reference-data/*.csv         ← 5월 워크북 데이터 (truth source)
        │
        ▼
[1] 책 매핑 룰 정의 (통장 코드 → book)
        │
        ▼
[2] 시스템_DB_스키마_v1.sql 작성 (이 문서 + 미결정 확정 후)
        │
        ▼
[3] Supabase 프로젝트 + RLS + audit_log 트리거
        │
        ▼
[4] CSV → seed 스크립트 (book 컬럼 부여하며 insert)
        │
        ▼
[5] 어드민 어플리케이션 구현
```
